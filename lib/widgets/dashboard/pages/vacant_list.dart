import 'package:flutter/material.dart';

class VacantList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xffC4DFCB),
      child: Center(child: Image.asset('assets/images/in_progress.png')),
    );
  }
}
